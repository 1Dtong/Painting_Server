const config = {
    database: {
        DATABASE: 'ai_drawing',
        USERNAME: 'root',
        PASSWORD: '629056dt',
        PORT: '3306',
        HOST: 'localhost'
    }
};

module.exports = config;
